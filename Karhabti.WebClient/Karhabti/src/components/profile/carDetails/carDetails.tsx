import './carDetails.scss';
import { ICarSubmitData } from '../../../types/types';

export default function CarDetails({ data }: { data: ICarSubmitData }) {
	return <div className="car-details-container"></div>;
}
